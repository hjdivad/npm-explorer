
import { keysFor } from 'npm-explorer/helpers/keys-for';
import { module, test } from 'qunit';

module('Unit | Helper | keys for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = keysFor([42]);
  assert.ok(result);
});

