
import { arrayIncludes } from 'npm-explorer/helpers/array-includes';
import { module, test } from 'qunit';

module('Unit | Helper | array includes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayIncludes([42]);
  assert.ok(result);
});

