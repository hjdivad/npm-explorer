
import { keysOf } from 'npm-explorer/helpers/keys-of';
import { module, test } from 'qunit';

module('Unit | Helper | keys of');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = keysOf([42]);
  assert.ok(result);
});

