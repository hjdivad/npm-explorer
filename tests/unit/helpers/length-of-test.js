
import { lengthOf } from 'npm-explorer/helpers/length-of';
import { module, test } from 'qunit';

module('Unit | Helper | length of');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lengthOf([42]);
  assert.ok(result);
});

